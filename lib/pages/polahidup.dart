import 'package:flutter/material.dart';

class RePolahidup extends StatelessWidget {
  const RePolahidup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
import 'package:flutter/material.dart';

class DetailsArtikel extends StatelessWidget {
  const DetailsArtikel({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      
    );
  }
}